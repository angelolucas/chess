import styled from 'styled-components';

export const App = styled.div({
  display: 'flex',
  background: '#11141a',
  color: '#dfe3ee',
  fontFamily: 'Roboto, Arial, sans-serif',
  minHeight: '100vh',
});
